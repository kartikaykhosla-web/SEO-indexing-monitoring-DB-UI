name: Indexing Monitor - onlymyhealth.com_hi

on:
  schedule:
    - cron: '25 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-monitor:
    uses: ./.github/workflows/indexing-monitor-shared.yml
    with:
      property: onlymyhealth.com_hi
      mode: all
      cutoff_datetime: ''
    secrets: inherit
