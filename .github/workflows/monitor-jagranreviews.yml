name: Indexing Monitor - jagranreviews.com

on:
  schedule:
    - cron: '15 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-monitor:
    uses: ./.github/workflows/indexing-monitor-shared.yml
    with:
      property: jagranreviews.com
      mode: all
      cutoff_datetime: ''
    secrets: inherit
