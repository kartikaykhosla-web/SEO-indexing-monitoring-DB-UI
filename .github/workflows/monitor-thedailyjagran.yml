name: Indexing Monitor - thedailyjagran.com

on:
  schedule:
    - cron: '30 * * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-monitor:
    uses: ./.github/workflows/indexing-monitor-shared.yml
    with:
      property: thedailyjagran.com
      mode: all
      cutoff_datetime: ''
    secrets: inherit
