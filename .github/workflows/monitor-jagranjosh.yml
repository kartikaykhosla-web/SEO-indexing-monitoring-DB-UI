name: Indexing Monitor - jagranjosh.com

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-monitor:
    uses: ./.github/workflows/indexing-monitor-shared.yml
    with:
      property: jagranjosh.com
      mode: all
      cutoff_datetime: ''
    secrets: inherit
